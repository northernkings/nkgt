{
  "name": "nkgt-2022",
  "version": "0.0.0",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "start": "yarn dev",
    "dev": "ts-node src/server",
    "prod": "yarn build && yarn prod:serve",
    "clean": "rimraf dist/",
    "build": "yarn clean && yarn build:vite && yarn build:prerender && yarn build:cleanup",
    "build:vite": "vite build && vite build --ssr",
    "build:prerender": "vite-plugin-ssr prerender",
    "build:cleanup": "rimraf dist/server/package.json",
    "prod:serve": "serve --config ../../serve.json dist/client/ --listen 3000",
    "test": "echo \"Error: no tests\" && exit 1"
  },
  "dependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.10",
    "@vitejs/plugin-vue": "^1.10.1",
    "@vue/compiler-sfc": "^3.2.23",
    "@vue/server-renderer": "^3.2.23",
    "cross-env": "^7.0.3",
    "express": "^4.17.1",
    "focus-visible": "^5.2.0",
    "ts-node": "^10.4.0",
    "typescript": "^4.5.2",
    "vite": "2.6.14",
    "vite-plugin-ssr": "^0.3.31",
    "vue": "^3.2.20"
  },
  "packageManager": "yarn@3.1.1",
  "devDependencies": {
    "rimraf": "^3.0.2",
    "serve": "^13.0.2"
  }
}
